# Milestone 1

To build:
```
make
```

To run server:
```
./build/server
```

To run client (in another terminal):
```
./build/client
```

The server listens on port 8080. Client connects and exchanges encrypted messages after authentication.
 If you're on Windows use WSL2 to run it.
